# ── Forge3D Vitrine – Environment Variables ───────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ── Port ──────────────────────────────────────────────────────────────────────
# Port for the Docker container (default: 80)
VITRINE_PORT=80

# ── Contact Form – EmailJS (Option 1 – Recommended) ──────────────────────────
# Create a free account at https://www.emailjs.com
# → Services → Add service → Gmail / SMTP
# → Email Templates → Create template (use variables: from_name, from_email, etc.)
# → Account → API Keys → Public Key
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxxxxxxxxxxx

# ── Contact Form – Formspree (Option 2 – Fallback) ───────────────────────────
# Create a free account at https://formspree.io
# Create a new form and copy the form ID (part after /f/)
# Example: https://formspree.io/f/xyzabc → VITE_FORMSPREE_ID=xyzabc
# Leave empty if using EmailJS
VITE_FORMSPREE_ID=

# ── Analytics – Google Analytics 4 (Optional) ────────────────────────────────
# Get your Measurement ID from Google Analytics console (GA4)
# Format: G-XXXXXXXXXX
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ── Meta Pixel (Optional) ─────────────────────────────────────────────────────
VITE_META_PIXEL_ID=
